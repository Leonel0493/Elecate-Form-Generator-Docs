Use Cases
=========

This section documents the application layer of the system, which contains the use cases that coordinate domain behavior.

Each use case is implemented as a **Command**, **Query**, or **Service**, depending on whether it performs write operations, read operations, or orchestration logic.

.. toctree::
   :maxdepth: 1

   generate_html_form
   save_prospect
   get_db_id
   generate_script
